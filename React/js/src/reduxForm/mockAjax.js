/*const sleep = payload => new Promise(resolve => setTimeout( () => resolve(true), payload ) )

export default (async function showRes(val){
	await sleep(500)
	//window.alert(val)
})*/

export default (val) => {
	alert(val)
}